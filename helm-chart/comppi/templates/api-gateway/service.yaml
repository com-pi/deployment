apiVersion: v1
kind: Service
metadata:
  name: api-gateway-svc
spec:
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: {{ default "8080" .Values.application.api_gateway.port }}
      targetPort: {{ default "8080" .Values.application.api_gateway.port }}

  type: {{ default "ClusterIP" .Values.application.api_gateway.serviceType }}

