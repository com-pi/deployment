{{- range $key, $value := .Values.mysql }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $key }}-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ $value.volumeMount.capacity }}
{{- end }}