replicaCount: 1

image:
  repository: utopiandrmer/comp-encyclo-service
  tag: latest

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080
  protocol: TCP

env:
  activeProfile: dev
  timezone: Asia/Seoul
